{% extends '@App/base_template.html.twig' %}

{% block body %}
    <h1>Niveau creation hh</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('niveau_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
