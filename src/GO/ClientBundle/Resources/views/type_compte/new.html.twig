{% extends 'base.html.twig' %}

{% block content_body %}
    <h1>Ajouter un nouveau type de compte</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Créer" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('type_compte_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
