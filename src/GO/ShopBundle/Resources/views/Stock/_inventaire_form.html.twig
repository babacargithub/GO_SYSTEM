{% if form is defined %}
  <div> 
    {{form_start(form)}}
    {{ form_widget(form)}}
{{form_rest(form)}}   
<input type="submit" class="btn btn-primary" />
{{form_end(form)}}  </div>

    {% endif %}

  