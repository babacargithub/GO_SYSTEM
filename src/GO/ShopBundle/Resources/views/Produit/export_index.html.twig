
    <div class="col-lg-6" id="details_client">
      <div class="col-lg-12 panel panel-primary" >
          <form action="{{ path("produit_export_all")}}" method="post">
  {{ form_widget(form)}}
  {{ form_row(form._token) }}
     {# {{ form_widget(form)}}#}
<input type="submit" class="btn btn-primary" />
          </form>
    <a href="{{ path("produit_export_all")}}">Exporter tous</a>
  </div>