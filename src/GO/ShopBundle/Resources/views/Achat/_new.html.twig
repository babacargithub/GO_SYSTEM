{% extends "GOShopBundle:Achat:achat_main_layout.html.twig" %}
      {% block content_body %}
        {% if form is defined %}
 <div class="col-lg-5" style="margin-left: 20px">
<form id="{{form.vars.name}}" action="{{path("facture_add_produit",{"id":facture.id})}}" class="form-horizontal" method="post" {{form_enctype(form)}}>
{{ form_widget(form)}}
    
<input type="submit" class="btn btn-primary" value="Enregistrer les modifications?"/>
</form>
</div>

    {% endif %}
    
{% endblock content_body %}

  