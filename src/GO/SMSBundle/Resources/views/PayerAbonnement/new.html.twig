{% extends '::base.html.twig' %}

{% block body -%}
    <h1>PayerAbonnement creation</h1>

    <form action="{{ path('payer_abonnement_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('payer_abonnement') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
