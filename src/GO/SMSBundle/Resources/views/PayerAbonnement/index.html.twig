{% extends '::base.html.twig' %}

{% block body -%}
    <h1>PayerAbonnement list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Montant</th>
                <th>Annee</th>
                <th>Date</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('payer_abonnement_show', { 'id': entity.id }) }}">{{ entity.montant }}</a></td>
                <td>{{ entity.annee }}</td>
                <td>{% if entity.date %}{{ entity.date|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('payer_abonnement_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('payer_abonnement_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('payer_abonnement_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
