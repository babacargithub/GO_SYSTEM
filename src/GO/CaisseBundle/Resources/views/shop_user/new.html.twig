{% extends '@GOCaisse/layout.html.twig' %}

{% block content_body %}
    <h1>Affecter un utilisateur à une boutique</h1>
<form action="{{path("shop_user_new")}}" method="post">
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}
</form>
    <ul>
        <li>
            <a href="{{ path('shop_user_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
