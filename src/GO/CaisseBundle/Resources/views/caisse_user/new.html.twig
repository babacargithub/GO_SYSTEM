{% extends '@GOCaisse/layout.html.twig' %}

{% block content_body %}
    <h1>Affecter Un Utilisateur à Une Caisse</h1>
<form action="{{path("caisse_user_new")}}" method="post">
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Affecter" />
    {{ form_end(form) }}
</form>
    <ul>
        <li>
            <a href="{{ path('caisse_user_index') }}">Retour à la liste</a>
        </li>
    </ul>
{% endblock %}
