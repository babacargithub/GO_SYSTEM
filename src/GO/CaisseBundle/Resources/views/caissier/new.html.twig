{% extends '@GOCaisse/caissier/caissier_main_layout.html.twig' %}

{% block content_body %}
    <h1>Créer Un nouveau caissier</h1>
<form action="{{path("caissier_new")}}" method="post">
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Enregistrer" />
    {{ form_end(form) }}
</form>
    <ul>
        <li>
            <a href="{{ path('caissier_index') }}">Retour à la liste</a>
        </li>
    </ul>
{% endblock %}
