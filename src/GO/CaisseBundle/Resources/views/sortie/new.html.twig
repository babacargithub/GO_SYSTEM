{% extends '@GOCaisse/sortie/sortie_main_layout.html.twig' %}

{% block content_body %}
    <div class="col-lg-4 col-md-6 col-xs-12">
        <h3 class="form-title">Enregistrer Une Nouvelle Sortie</h3>
        <form action="{{path("sortie_new")}}" method="post">
        {{ form_start(form) }}
            {{ form_widget(form) }}
         <input type="submit" value="Enregistrer" />
        {{ form_end(form) }}
        </form>
        <ul>
            <li>
                <a href="{{ path('sortie_index') }}">Retour</a>
            </li>
        </ul>
    </div>
{% endblock %}
