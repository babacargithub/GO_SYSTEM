{% extends '@GOCaisse/caisse/caisse_main_layout.html.twig' %}

{% block content_body %}
    <h1>Caisse</h1>

    <table class="table table-bordered table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ caisse.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ caisse.libelle }}</td>
            </tr>
            <tr>
                <th>Solde</th>
                <td>{{ caisse.solde }}</td>
            </tr>
            <tr>
                <th>Lastop</th>
                <td>{% if caisse.lastOp %}{{ caisse.lastOp|date('Y-m-d H:i:s') }}{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('caisse_pro_index') }}">Retour à la liste</a>
        </li>
        <li>
            <a href="{{ path('caisse_pro_edit', { 'id': caisse.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
